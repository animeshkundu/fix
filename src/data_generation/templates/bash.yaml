# Bash shell command templates for training data generation
# Format: each entry has correct command, common errors, and natural language descriptions

shell: bash
weight: 0.35  # 35% of total data

# Navigation commands
navigation:
  - correct: "cd {path}"
    errors:
      - "cd{path}"  # Missing space
      - "cd ..{path}"  # Extra dots
      - "cd {typo_path}"  # Typo in path
    natural_language:
      - "go to {path}"
      - "change directory to {path}"
      - "navigate to {path}"

  - correct: "cd .."
    errors:
      - "cd.."
      - "cd..."
      - "cd ."
    natural_language:
      - "go up one directory"
      - "parent directory"
      - "go back"

  - correct: "cd ~"
    errors:
      - "cd~"
      - "cd $HOME"
      - "cd /home"
    natural_language:
      - "go to home"
      - "go home"
      - "home directory"

  - correct: "mkdir -p {path}"
    errors:
      - "mkdir {path}"
      - "mkidr -p {path}"
      - "mkdir-p {path}"
    natural_language:
      - "create directory {path}"
      - "make folder {path}"
      - "create {path} folder"

  - correct: "pwd"
    errors:
      - "pdw"
      - "pws"
    natural_language:
      - "where am i"
      - "current directory"
      - "print working directory"

# File operations
file_operations:
  - correct: "ls -la"
    errors:
      - "ls -al"
      - "ls -l -a"
      - "la -ls"
      - "sl -la"
    natural_language:
      - "list all files"
      - "show all files with details"
      - "list hidden files"

  - correct: "ls -lh"
    errors:
      - "ls -hl"
      - "ls -l -h"
    natural_language:
      - "list files with human readable sizes"
      - "show file sizes"

  - correct: "cat {file}"
    errors:
      - "cta {file}"
      - "cat{file}"
    natural_language:
      - "show contents of {file}"
      - "print {file}"
      - "display {file}"

  - correct: "head -n {n} {file}"
    errors:
      - "head -{n} {file}"
      - "head -n{n} {file}"
    natural_language:
      - "show first {n} lines of {file}"
      - "first {n} lines"

  - correct: "tail -f {file}"
    errors:
      - "tail -F {file}"
      - "tail --follow {file}"
    natural_language:
      - "follow {file}"
      - "watch {file} changes"
      - "stream {file}"

  - correct: "cp -r {src} {dst}"
    errors:
      - "cp {src} {dst}"
      - "cp -R {src} {dst}"
      - "copy {src} {dst}"
    natural_language:
      - "copy {src} to {dst}"
      - "copy directory {src} to {dst}"

  - correct: "mv {src} {dst}"
    errors:
      - "move {src} {dst}"
      - "mv {src}{dst}"
    natural_language:
      - "move {src} to {dst}"
      - "rename {src} to {dst}"

  - correct: "rm -rf {path}"
    errors:
      - "rm -r {path}"
      - "rm -f {path}"
      - "rm {path}"
    natural_language:
      - "delete {path}"
      - "remove {path} recursively"
      - "force delete {path}"

  - correct: "chmod +x {file}"
    errors:
      - "chmod x {file}"
      - "chmod +X {file}"
      - "chmod 755 {file}"
    natural_language:
      - "make {file} executable"
      - "add execute permission to {file}"

  - correct: "chown {user}:{group} {file}"
    errors:
      - "chown {user} {file}"
      - "chown {user}.{group} {file}"
    natural_language:
      - "change owner of {file} to {user}"

# Search commands
search:
  - correct: "find . -name '{pattern}'"
    errors:
      - "find -name '{pattern}'"
      - "find . -name {pattern}"
      - "find '{pattern}'"
    natural_language:
      - "find files named {pattern}"
      - "search for {pattern}"

  - correct: "find . -type f -size +{size}M"
    errors:
      - "find . -size +{size}M"
      - "find -type f -size {size}M"
    natural_language:
      - "find files larger than {size}M"
      - "find large files"

  - correct: "grep -r '{pattern}' ."
    errors:
      - "grep '{pattern}' ."
      - "grep -R '{pattern}' ."
      - "gerp -r '{pattern}' ."
    natural_language:
      - "search for {pattern} in all files"
      - "grep for {pattern} recursively"
      - "find {pattern} in code"

  - correct: "grep -i '{pattern}' {file}"
    errors:
      - "grep '{pattern}' {file}"
      - "grep -I '{pattern}' {file}"
    natural_language:
      - "search for {pattern} case insensitive"

# Process management
processes:
  - correct: "ps aux"
    errors:
      - "ps -aux"
      - "pa aux"
      - "ps aux | grep"
    natural_language:
      - "show all processes"
      - "list running processes"
      - "what's running"

  - correct: "ps aux | grep {process}"
    errors:
      - "ps -aux | grep {process}"
      - "ps aux | gerp {process}"
    natural_language:
      - "find {process} process"
      - "is {process} running"

  - correct: "kill -9 {pid}"
    errors:
      - "kill {pid}"
      - "kill -KILL {pid}"
      - "kil -9 {pid}"
    natural_language:
      - "force kill process {pid}"
      - "kill {pid}"

  - correct: "pkill {name}"
    errors:
      - "pkil {name}"
      - "kill {name}"
    natural_language:
      - "kill all {name} processes"
      - "stop {name}"

  - correct: "top"
    errors:
      - "tpo"
      - "topp"
    natural_language:
      - "show system monitor"
      - "cpu usage"
      - "memory usage"

  - correct: "htop"
    errors:
      - "htopp"
      - "htpo"
    natural_language:
      - "interactive process viewer"
      - "better top"

# Network commands
network:
  - correct: "curl -X GET {url}"
    errors:
      - "curl {url}"
      - "curl -XGET {url}"
      - "crul -X GET {url}"
    natural_language:
      - "fetch {url}"
      - "get {url}"
      - "http get {url}"

  - correct: "curl -X POST -d '{data}' {url}"
    errors:
      - "curl -X POST {url} -d '{data}'"
      - "curl -d '{data}' {url}"
    natural_language:
      - "post {data} to {url}"
      - "send data to {url}"

  - correct: "wget {url}"
    errors:
      - "wegt {url}"
      - "wget -O {url}"
    natural_language:
      - "download {url}"
      - "fetch and save {url}"

  - correct: "ssh {user}@{host}"
    errors:
      - "ssh {host}"
      - "shh {user}@{host}"
      - "ssh {user} {host}"
    natural_language:
      - "connect to {host} as {user}"
      - "ssh into {host}"

  - correct: "scp {src} {user}@{host}:{dst}"
    errors:
      - "scp {src} {host}:{dst}"
      - "scp {src} {user}@{host}"
    natural_language:
      - "copy {src} to {host}"
      - "upload {src} to {host}"

  - correct: "ping -c 4 {host}"
    errors:
      - "ping {host}"
      - "ping -c4 {host}"
    natural_language:
      - "ping {host}"
      - "check if {host} is reachable"

  - correct: "netstat -tulpn"
    errors:
      - "nestat -tulpn"
      - "netstat -tlpn"
    natural_language:
      - "show open ports"
      - "list listening ports"

  - correct: "ss -tulpn"
    errors:
      - "ss -tlpn"
      - "ss tulpn"
    natural_language:
      - "show sockets"
      - "list connections"

# Git commands
git:
  - correct: "git status"
    errors:
      - "gti status"
      - "git stauts"
      - "git stats"
      - "git st"
    natural_language:
      - "check git status"
      - "what changed"
      - "show changes"

  - correct: "git add ."
    errors:
      - "git add"
      - "git add -a"
      - "git add -A"
    natural_language:
      - "stage all changes"
      - "add all files"

  - correct: "git commit -m '{message}'"
    errors:
      - "git commit '{message}'"
      - "git commit -m{message}"
      - "git comit -m '{message}'"
    natural_language:
      - "commit with message {message}"
      - "save changes as {message}"

  - correct: "git push origin {branch}"
    errors:
      - "git push"
      - "git push {branch}"
      - "git psuh origin {branch}"
    natural_language:
      - "push to {branch}"
      - "upload changes to {branch}"

  - correct: "git pull origin {branch}"
    errors:
      - "git pull"
      - "git pull {branch}"
      - "git pul origin {branch}"
    natural_language:
      - "pull from {branch}"
      - "get latest from {branch}"
      - "update from {branch}"

  - correct: "git checkout {branch}"
    errors:
      - "git checkotu {branch}"
      - "git checkout"
      - "git co {branch}"
    natural_language:
      - "switch to {branch}"
      - "go to {branch}"

  - correct: "git checkout -b {branch}"
    errors:
      - "git checkout -B {branch}"
      - "git branch {branch} && git checkout {branch}"
    natural_language:
      - "create and switch to {branch}"
      - "new branch {branch}"

  - correct: "git merge {branch}"
    errors:
      - "git merg {branch}"
      - "git merge"
    natural_language:
      - "merge {branch}"
      - "combine {branch}"

  - correct: "git rebase {branch}"
    errors:
      - "git reabse {branch}"
      - "git rebase"
    natural_language:
      - "rebase onto {branch}"
      - "rebase from {branch}"

  - correct: "git stash"
    errors:
      - "git stach"
      - "git stas"
    natural_language:
      - "stash changes"
      - "save work temporarily"

  - correct: "git stash pop"
    errors:
      - "git stash apply"
      - "git stas pop"
    natural_language:
      - "restore stashed changes"
      - "pop stash"

  - correct: "git log --oneline"
    errors:
      - "git log --online"
      - "git log -1"
    natural_language:
      - "show commit history"
      - "git history"

  - correct: "git diff"
    errors:
      - "git dif"
      - "git diff."
    natural_language:
      - "show differences"
      - "what changed"

  - correct: "git clone {url}"
    errors:
      - "git clone"
      - "git cloen {url}"
    natural_language:
      - "clone {url}"
      - "download repo {url}"

# Package management
packages:
  - correct: "sudo apt update && sudo apt upgrade -y"
    errors:
      - "apt update && apt upgrade"
      - "sudo apt update"
      - "apt-get update"
    natural_language:
      - "update system"
      - "upgrade packages"
      - "update all"

  - correct: "sudo apt install {package}"
    errors:
      - "apt install {package}"
      - "sudo apt-get install {package}"
      - "sudo apt instal {package}"
    natural_language:
      - "install {package}"
      - "add {package}"

  - correct: "pip install {package}"
    errors:
      - "pip instal {package}"
      - "pip istall {package}"
      - "pip3 install {package}"
    natural_language:
      - "install python package {package}"
      - "pip {package}"

  - correct: "pip install -r requirements.txt"
    errors:
      - "pip install requirements.txt"
      - "pip install -r requirements"
    natural_language:
      - "install requirements"
      - "install dependencies"

  - correct: "npm install {package}"
    errors:
      - "npm instal {package}"
      - "npm i {package}"
      - "npm add {package}"
    natural_language:
      - "install npm package {package}"
      - "add {package} to project"

  - correct: "npm install"
    errors:
      - "npm i"
      - "npm instal"
    natural_language:
      - "install dependencies"
      - "npm install all"

# Compression
compression:
  - correct: "tar -xzf {file}"
    errors:
      - "tar -xvf {file}"
      - "tar xzf {file}"
      - "untar {file}"
    natural_language:
      - "extract {file}"
      - "unzip {file}"
      - "decompress {file}"

  - correct: "tar -czf {archive}.tar.gz {dir}"
    errors:
      - "tar -cvf {archive}.tar.gz {dir}"
      - "tar czf {archive} {dir}"
    natural_language:
      - "compress {dir}"
      - "create archive of {dir}"
      - "tar {dir}"

  - correct: "unzip {file}"
    errors:
      - "unzpi {file}"
      - "unzip -d {file}"
    natural_language:
      - "unzip {file}"
      - "extract zip {file}"

  - correct: "zip -r {archive}.zip {dir}"
    errors:
      - "zip {archive}.zip {dir}"
      - "zip -r {archive} {dir}"
    natural_language:
      - "zip {dir}"
      - "compress {dir} to zip"

# Text processing
text:
  - correct: "sed -i 's/{old}/{new}/g' {file}"
    errors:
      - "sed 's/{old}/{new}/g' {file}"
      - "sed -i 's/{old}/{new}/' {file}"
    natural_language:
      - "replace {old} with {new} in {file}"
      - "find and replace in {file}"

  - correct: "awk '{print $1}' {file}"
    errors:
      - "awk {print $1} {file}"
      - "awk '{print}' {file}"
    natural_language:
      - "get first column from {file}"
      - "extract column 1"

  - correct: "sort -u {file}"
    errors:
      - "sort {file} | uniq"
      - "sort -U {file}"
    natural_language:
      - "sort and remove duplicates"
      - "unique sorted"

  - correct: "wc -l {file}"
    errors:
      - "wc {file}"
      - "wc -L {file}"
    natural_language:
      - "count lines in {file}"
      - "how many lines"

  - correct: "cut -d',' -f1 {file}"
    errors:
      - "cut -d, -f1 {file}"
      - "cut -f1 {file}"
    natural_language:
      - "get first field from csv"
      - "extract first column"

# Disk and system
system:
  - correct: "df -h"
    errors:
      - "df"
      - "df -H"
      - "dh -f"
    natural_language:
      - "disk space"
      - "how much disk space"
      - "check storage"

  - correct: "du -sh {dir}"
    errors:
      - "du -h {dir}"
      - "du {dir}"
    natural_language:
      - "size of {dir}"
      - "how big is {dir}"

  - correct: "free -h"
    errors:
      - "free"
      - "free -m"
    natural_language:
      - "memory usage"
      - "how much ram"
      - "available memory"

  - correct: "uname -a"
    errors:
      - "uname"
      - "uname -r"
    natural_language:
      - "system info"
      - "kernel version"
      - "os info"

  - correct: "whoami"
    errors:
      - "who am i"
      - "whomi"
    natural_language:
      - "current user"
      - "who am i"

  - correct: "hostname"
    errors:
      - "hostnmae"
      - "hostname -f"
    natural_language:
      - "machine name"
      - "computer name"

# Docker
docker:
  - correct: "docker ps"
    errors:
      - "docker pa"
      - "docker ls"
    natural_language:
      - "list containers"
      - "running containers"

  - correct: "docker ps -a"
    errors:
      - "docker ps --all"
      - "docker pa -a"
    natural_language:
      - "all containers"
      - "list all docker containers"

  - correct: "docker images"
    errors:
      - "docker image"
      - "docker imgs"
    natural_language:
      - "list images"
      - "docker images"

  - correct: "docker run -it {image}"
    errors:
      - "docker run {image}"
      - "docker run -i -t {image}"
    natural_language:
      - "run {image} interactively"
      - "start {image}"

  - correct: "docker exec -it {container} bash"
    errors:
      - "docker exec {container} bash"
      - "docker exec -it {container}"
    natural_language:
      - "shell into {container}"
      - "enter {container}"

  - correct: "docker stop {container}"
    errors:
      - "docker stopp {container}"
      - "docker kill {container}"
    natural_language:
      - "stop {container}"
      - "shutdown {container}"

  - correct: "docker rm {container}"
    errors:
      - "docker remove {container}"
      - "docker delete {container}"
    natural_language:
      - "remove container {container}"
      - "delete container"

  - correct: "docker rmi {image}"
    errors:
      - "docker rm {image}"
      - "docker image rm {image}"
    natural_language:
      - "remove image {image}"
      - "delete image"

  - correct: "docker-compose up -d"
    errors:
      - "docker compose up -d"
      - "docker-compose up"
    natural_language:
      - "start services"
      - "docker compose up"

  - correct: "docker system prune -a"
    errors:
      - "docker prune"
      - "docker system prune"
    natural_language:
      - "clean up docker"
      - "remove unused docker stuff"

# Kubernetes
kubernetes:
  - correct: "kubectl get pods"
    errors:
      - "kubectl get pod"
      - "kubeclt get pods"
      - "kubectl pods"
    natural_language:
      - "list pods"
      - "show pods"

  - correct: "kubectl get pods -A"
    errors:
      - "kubectl get pods --all-namespaces"
      - "kubectl get pods -a"
    natural_language:
      - "all pods"
      - "pods in all namespaces"

  - correct: "kubectl describe pod {pod}"
    errors:
      - "kubectl describe {pod}"
      - "kubectl desc pod {pod}"
    natural_language:
      - "describe {pod}"
      - "pod details {pod}"

  - correct: "kubectl logs {pod}"
    errors:
      - "kubectl log {pod}"
      - "kubeclt logs {pod}"
    natural_language:
      - "show logs for {pod}"
      - "pod logs"

  - correct: "kubectl exec -it {pod} -- bash"
    errors:
      - "kubectl exec {pod} bash"
      - "kubectl exec -it {pod} bash"
    natural_language:
      - "shell into pod {pod}"
      - "enter pod"

  - correct: "kubectl apply -f {file}"
    errors:
      - "kubectl apply {file}"
      - "kubectl create -f {file}"
    natural_language:
      - "apply {file}"
      - "deploy {file}"

# Permission errors (sudo patterns)
sudo_patterns:
  - correct: "sudo {command}"
    trigger_errors:
      - "Permission denied"
      - "Operation not permitted"
      - "must be root"
      - "EACCES"
    commands_needing_sudo:
      - "apt install"
      - "apt update"
      - "systemctl restart"
      - "systemctl start"
      - "systemctl stop"
      - "chown"
      - "chmod"
      - "mount"
      - "umount"
