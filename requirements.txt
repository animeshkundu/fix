# Core dependencies
pyyaml>=6.0
jinja2>=3.1
anthropic>=0.30
tiktoken>=0.5

# CLI and utilities
typer>=0.12
rich>=13.0
tqdm>=4.66

# Training (Apple Silicon - MLX)
mlx>=0.12
mlx-lm>=0.12

# Training (Alternative - PyTorch with MPS)
# torch>=2.0
# transformers>=4.40
# peft>=0.10
# trl>=0.8
# datasets>=2.19
# accelerate>=0.29

# Inference (Local CLI)
llama-cpp-python>=0.2
gguf>=0.6

# Development
pytest>=8.0
ruff>=0.4
